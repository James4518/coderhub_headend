import { styled } from 'styled-components';

export const RegisterWrapper = styled.section`
  #basic {
    margin: 0 auto;
    padding: 50px 0;
  }
  h2 {
    text-align: center;
    font-size: 1.8rem;
  }
`;
