import { styled } from 'styled-components';

export const LoginWrapper = styled.section`
  h2 {
    text-align: center;
    font-size: 1.8rem;
  }
  #basic {
    margin: 0 auto;
    padding: 50px 0;
  }
`;
