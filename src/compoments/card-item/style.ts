import { styled } from 'styled-components';

export const CardItemWrapper = styled.div`
  .current {
    color: #1d2129;
    font-weight: 600;
    font-size: 32px;
    line-height: 42px;
    margin-bottom: 4px;
    white-space: nowrap;
  }
`;
